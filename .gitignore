node_modules/
dist/
ui/dist/
.env
.env.local
*.log
coverage/
data/blobs/
sidecar/__pycache__/
sidecar/.venv/
*.pyc
.DS_Store
Thumbs.db
# Fixtures are synthetic or de-identified, but never commit anything client-derived.
test/fixtures/live/
ui/tsconfig.tsbuildinfo
*.tsbuildinfo
vendor/
.router/

# Redis's snapshot, written into whatever directory redis-server was started from — which is
# the repo root when someone brings the stack up by hand to look at the UI. Runtime state, and
# on a real box it would hold queued job payloads.
dump.rdb
